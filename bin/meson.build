if have_bdb
    subdir('ad')
endif

if have_libgcrypt
    subdir('afppasswd')
endif

subdir('misc')

if have_appletalk
    subdir('aecho')
    subdir('getzones')
    subdir('nbp')
    subdir('pap')
    subdir('rtmpqry')
endif
