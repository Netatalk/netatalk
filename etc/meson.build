if have_spotlight
    subdir('spotlight')
endif

if use_dbd_backend
    subdir('cnid_dbd')
endif

subdir('afpd')
subdir('netatalk')
subdir('uams')

if have_appletalk
    subdir('atalkd')
    subdir('papd')
endif
