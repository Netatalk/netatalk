if have_spotlight
    subdir('spotlight')
endif

subdir('afpd')
subdir('netatalk')
subdir('uams')

if use_dbd_backend
    subdir('cnid_dbd')
endif

if have_appletalk
    subdir('atalkd')
    subdir('papd')
endif
